# pravartak: template=gitignore.template version={{PRAVARTAK_VERSION}} generated={{SCAFFOLD_DATE}}
#
# Rendered by /scaffold for {{PROJECT_NAME}} ({{LANGUAGE}}). Edit freely.

# OS / editor
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Claude adapter per-user local overrides (not shared)
.claude/settings.local.json

# Language isolation (project-local; never commit the environment itself)
.venv/
node_modules/
target/
dist/
build/
__pycache__/
*.py[cod]

# Test / coverage artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# Secrets — never commit (security baseline §13.10)
.env
.env.*
*.pem
*.key
